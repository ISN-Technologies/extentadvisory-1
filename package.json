{
  "name": "extentadvisory",
  "homepage": "https://extentadvisory.co.tz",
  "version": "2021.1.0",
  "description": "We are a leading law firm in Tanzania",
  "author": "ISN Technologies",
  "private": true,
  "scripts": {
    "dev": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt ",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt build",
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt start ",
    "generate": "export NODE_OPTIONS=--openssl-legacy-provider && nuxt generate "
  },
  "dependencies": {
    "fs": "0.0.2",
    "nuxt": "^2.0.0",
    "path": "^0.12.7",
    "swiper": "^5.4.5",
    "v-owl-carousel": "^1.0.8",
    "vue-awesome-swiper": "^4.1.1",
    "vue-count-to": "^1.0.13",
    "vue-observe-visibility": "^0.4.6",
    "vue-slick-carousel": "^1.0.6"
  },
  "file.watcherExclude" : {
  "**/.git/objects/**" : true,
  "**/node_modules/**" : true
  }
}
